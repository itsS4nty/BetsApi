import { IAuth } from 'interfaces/auth/IAuth';

export interface ISetUser {
    firstName: string;
    lastName: string;
    age: number;
    userName: string;
    mail: string;
    passwd: string;
}
